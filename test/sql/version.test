# name: test/sql/version.test
# description: test netquack extension version function
# group: [netquack]

require netquack

# Test that version function returns a version string
query I
SELECT * FROM netquack_version();
----
v1.7.0
